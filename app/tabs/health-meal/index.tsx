import Container from '@/common/components/container'
import { StyleSheet } from 'react-native'

export default function HealthMeal() {
    return <Container>食谱</Container>
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
})
